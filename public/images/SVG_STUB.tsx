/** @jsxRuntime classic */
/** @jsx jsx */
import React from 'react'
import { jsx, Flex, Button, useThemeUI, Styled } from 'theme-ui'

type SVGColorWrap = {
  color?: string
  svg: React.ReactElement
}

const Modal = ({color}: SVGColorWrap) => {
  const { theme } = useThemeUI()
  return (
    <React.Fragment>

    </React.Fragment>
  )
}

export default Modal
