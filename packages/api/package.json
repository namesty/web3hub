{
  "name": "@web3hub/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "rm -rf .next out && next build && next export",
    "deploy": "cd out && surge && cd .."
  },
  "dependencies": {
    "express": "4.17.1"
  },
  "devDependencies": {
    "typescript": "^4.0.5"
  }
}
